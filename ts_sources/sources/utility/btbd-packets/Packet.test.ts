declare var describe;
declare var expect;
declare var test;

import { Packet } from './Packet';

describe('Packet Test Suite', () => {

    test('placeholder', () => {
        const pack = new Packet('hey');
    });

});
